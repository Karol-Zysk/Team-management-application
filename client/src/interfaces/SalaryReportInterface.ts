export interface SalaryReportInterface {
  clockifyId: string;
  clockifyName: string;
  createdAt: string;
  email: string;
  firstName: string;
  hourlyRate: number;
  hoursWorked: number;
  id: string;
  lastName: string;
  profilePicture: string;
  salary: number;
  updatedAt: string;
  userId: string;
  workspaceId: string;
}
